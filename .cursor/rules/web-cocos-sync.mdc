---
description: 默认只维护 Web；Cocos 仅在明确要求时同步
alwaysApply: true
---

# Web 优先 / Cocos 按需

本仓库有两套客户端：`client/`（Web）与 `cocos/`（Cocos）。

## 当前策略（默认）

1. **后续功能与体验改动默认只做 Web**（`client/`），以及权威逻辑 `server/` + `shared/`。
2. **不要改 Cocos**，除非用户明确提到 Cocos / 双端同步 / 小游戏导出等。
3. 改 `shared/` 规则或节奏时：仍可跑 `node tools/syncCocosLib.mjs` 保持规则文件不漂移，但**不必**同步 Cocos UI / 交互实现，也不必构建发布 `publish/cocos`。
4. 若用户点名要动 Cocos，再按下方对照表补齐，并在回复中标明 Web 已先行、本次补的是 Cocos。

## 同步对照（仅在明确要求 Cocos 时使用）

| 类型 | Web | Cocos | 同步方式 |
|------|-----|-------|----------|
| 规则 / 节奏常量 | `shared/` | `cocos/.../rules/` | `node tools/syncCocosLib.mjs` |
| 联机协议 / API | `client/src/net.ts` | `cocos/.../Net.ts` | 接口同名同语义 |
| 大厅/房间/结算/引导 UI | `client/index.html` + `main.ts` | `LobbyUI.ts` | 能力对齐 |
| 牌桌交互与反馈 | `client/src/table.ts` + `main.ts` | `GameEntry.ts` + `CardNode.ts` | 行为对齐 |
| 音效 / BGM | `client/src/audio.ts` | （可选） | Web 优先 |

## 服务端

服务端权威逻辑只改 `server/` + `shared/`；客户端只发意图、只做展示。
